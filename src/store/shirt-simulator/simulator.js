import { defineStore } from 'pinia'
import API from '@/api/simulator'

export const useShirtSimulatorStore = defineStore({
    id: 'shirt-simulator',
    state: () => ({

    }),
    getters: {

    },
    actions: {
        
    }
})